export function addTwoNums(num1,num2){
    return num1 + num2;
}
export {multiply} from './multiply';//Inline import them export example
//Note Exporting to other cmp can be done only from main JS file like this one. 